import { CheckIsRegistrationPipe } from './check-is-registration.pipe';

describe('CheckIsRegistrationPipe', () => {
  it('should be defined', () => {
    expect(new CheckIsRegistrationPipe()).toBeDefined();
  });
});
